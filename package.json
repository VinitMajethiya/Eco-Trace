{
  "name": "ecotrace",
  "version": "1.0.0",
  "description": "EcoTrace - Personal Carbon Footprint Tracker & Reduction Coach",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client --legacy-peer-deps",
    "migrate": "npm run migrate --prefix server",
    "seed": "npm run seed --prefix server",
    "test-server": "npm run test --prefix server",
    "dev-server": "npm start --prefix server",
    "dev-client": "npm run dev --prefix client"
  }
}
